package com.test.enigma.ui.movies.detail

import com.test.enigma.databinding.ActivityMovieDetailBinding

fun MovieDetailActivity.setupYoutubePlayer(binding: ActivityMovieDetailBinding) {
    binding.youtubePlayerView.enableAutomaticInitialization = true

//    binding.youtubePlayerView.addYouTubePlayerListener(youTubePlayerListener = youtubeListener)
}